Создадим файл ```.env``` и внесем туда значения переменных:
```
POSTGRES_USER=devops
POSTGRES_PASSWORD=mysecret
```
При запуске проекта в качестве env-файла можно указать файл с любым именем через параметр:
```
--env-file имя_файла
```
