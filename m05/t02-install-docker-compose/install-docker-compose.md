Для современных версий «Docker Compose» используется команда:
```bash
docker compose version
```

Для устаревшей версии «docker compose» используется команда:
```bash
docker-compose --version
```

Пример для современной версии:
```bash
$ docker compose version
Docker Compose version v5.1.4
```
