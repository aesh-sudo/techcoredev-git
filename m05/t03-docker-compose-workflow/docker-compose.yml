services:
  app:
    build:
      dockerfile: dockerfile
      context: .
    ports:
      - '5000:5000'
  db:
    image: postgres:14-alpine
    environment:
      - 'POSTGRES_PASSWORD=mysecret'
    ports:
      - '5432:5432'
